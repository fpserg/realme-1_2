# Domain layer

Ontology, evidence, admission, temporal and projection rules belong here. Domain code is pure TypeScript and must not depend on application, framework, UI, database, provider or platform modules.
